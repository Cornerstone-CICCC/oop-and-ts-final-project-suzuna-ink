---
/**
 * Design reference for task cards.
 * Cards are created dynamically at runtime by JavaScript (main.ts).
 * All task card styles are in src/styles/global.css (global scope).
 *
 * HTML structure for dynamically created cards:
 *   <div class="task-card" draggable="true" data-task-id="{id}">
 *     <h3 class="task-title">{title}</h3>
 *     <p class="task-due-date">{dueDate}</p>
 *   </div>
 */
---

